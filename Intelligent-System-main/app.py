import streamlit as st
import app as app
import pandas as pd

def show():
   
    st.header("Welcome to Intellgent System and ML & Neural Network Web")

    # 👉 สร้าง Tabs Menu
    tabs = st.tabs(["Dataset", "Datapcessing", "📊 Development","🤖 ทฤษฎี Machine Learning (ML) ","🧠 ทฤษฎี Neural Network (NN)"])

    with tabs[0]:
        st.header("DataSet(ML)",divider=True)
        st.header("📂ที่มาของ Dataset:")
        st.text("⚪ข้อมูลนี้เป็นชุดข้อมูลตัวอย่างที่มักใช้ในการศึกษา Machine Learning สำหรับธุรกิจธนาคาร")
        st.text("⚪ประกอบไปด้วยข้อมูลลูกค้า 10,000 ราย จากธนาคารหนึ่ง")
        st.text("⚪มีคุณสมบัติของลูกค้า เช่น")
        st.text("⚪อายุ (Age), เพศ (Gender)")
        st.text("⚪สถานที่อยู่อาศัย (Geography) เช่น France, Germany, Spain")
        st.text("⚪คะแนนเครดิต (Credit Score)")
        st.text("⚪จำนวนผลิตภัณฑ์ที่ใช้ (NumOfProducts)")
        st.text("⚪ยอดเงินคงเหลือในบัญชี (Balance)")
        st.text("⚪เป็นสมาชิกที่ยังใช้งานอยู่หรือไม่ (IsActiveMember)")
        st.header("📌จุดประสงค์ของ Dataset:")
        st.text("⚪ใช้สำหรับการพยากรณ์ว่าลูกค้าธนาคาร จะเลิกใช้บริการ (Churn) หรือไม่")
        st.text("⚪สามารถนำไปใช้เพื่อช่วย เพิ่ม retention rate โดยการคาดการณ์กลุ่มลูกค้าที่มีความเสี่ยงสูง")
        st.text("⚪มีเป้าหมายหลักคือการช่วยให้ธุรกิจวางแผนกลยุทธ์ รักษาฐานลูกค้า ได้ดีขึ้น")
        st.markdown("[แหล่งที่มา](https://www.kaggle.com/datasets/rikdifos/credit-card-approval-prediction?resource=download)")
        df = pd.read_csv("Churn Modeling.csv")
        st.write("### ตัวอย่างข้อมูล")
        st.dataframe(df.head())


        st.header("DataSet(NN)",divider=True)
        st.header("📂ที่มาของ Dataset:")
        st.text("⚪ข้อมูลนี้รวบรวมจากแหล่งข้อมูลทางการเงิน เช่น Yahoo Finance หรือ Bloomberg")
        st.text("⚪มี ข้อมูลราคาหุ้นของ USO และดัชนีตลาดที่เกี่ยวข้อง รวมถึง")
        st.text("⚪ราคาหุ้นเปิด-ปิด (Open, Close, Adj Close)")
        st.text("⚪ราคาสูงสุด-ต่ำสุด (High, Low)")
        st.text("⚪ปริมาณการซื้อขาย (Volume)")
        st.text("⚪ดัชนีที่เกี่ยวข้อง เช่น S&P 500, Dow Jones, น้ำมันดิบ Brent และ WTI")
        st.header("📌จุดประสงค์ของ Dataset:")
        st.text("⚪ใช้สำหรับ การพยากรณ์ราคาหุ้น USO (United States Oil Fund)")
        st.text("⚪ช่วยให้นักลงทุนสามารถใช้ Machine Learning เพื่อทำนาย ราคาปิดที่ปรับปรุงแล้ว (USO_Adj Close)")
        st.text("⚪สามารถนำไปพัฒนา Trading Algorithms หรือ การวิเคราะห์แนวโน้มตลาดน้ำมัน")
        st.markdown("[แหล่งที่มา](https://www.kaggle.com/datasets/sid321axn/gold-price-prediction-dataset/data)")
        df = pd.read_csv("FINAL_USO.csv")
        st.write("### ตัวอย่างข้อมูล")
        st.dataframe(df.head())


    with tabs[1]:
        st.header("1️⃣ โมเดลที่ใช้กับ FINAL_USO.csv 🔥",divider=True)
        st.text("🔻ปัญหาที่แก้:")
        st.text("🔻ทำนายราคาปิดที่ปรับปรุงแล้ว (USO_Adj Close) ของกองทุน USO (United States Oil Fund)")
        st.text("🔻ใช้ข้อมูลราคาหุ้นและดัชนีตลาดเพื่อพยากรณ์")
        st.header("🔹 ขั้นตอนการพัฒนาโมเดล:",divider=True)
        st.text("🔻โหลดและเตรียมข้อมูล")
        st.text("🔻ลบคอลัมน์ Date ออก เนื่องจากไม่เกี่ยวข้องกับโมเดล")
        st.text("🔻แยกข้อมูลเป็น X (Features) และ y (Target) โดยใช้ USO_Adj Close เป็นค่าที่ต้องการพยากรณ์")
        st.text("🔻ใช้ StandardScaler เพื่อปรับขนาดข้อมูลให้อยู่ในช่วงมาตรฐาน")
        st.text("🔻แบ่งชุดข้อมูล (Train/Test Split)")
        st.text("🔻แบ่งข้อมูลออกเป็น 80% ฝึกสอน (Train) และ 20% ทดสอบ (Test)")
        st.text("🔻พัฒนาโมเดลหลายแบบเพื่อเปรียบเทียบ")
        st.text("🔻Linear Regression")
        st.text("🔻Decision Tree Regressor")
        st.text("🔻Random Forest Regressor")
        st.text("🔻Support Vector Machine (SVR)")
        st.text("🔻K-Nearest Neighbors (KNN)")
        st.text("🔻ใช้ GridSearchCV หาค่า n_neighbors ที่ดีที่สุดสำหรับ KNN")
        st.text("🔻วัดผลโมเดล")
        st.text("🔻ใช้ Mean Absolute Error (MAE), Mean Squared Error (MSE) และ R² Score เพื่อเปรียบเทียบประสิทธิภาพแต่ละโมเดล")
        st.text("🔻พล็อตกราฟ Actual vs Predicted เพื่อดูแนวโน้มของผลลัพธ์")
        st.text("🔻บันทึกโมเดลที่ดีที่สุด")
        st.text("🔻โมเดล KNN และตัวปรับสเกล (StandardScaler) ถูกบันทึกเป็น .pkl และสามารถดาวน์โหลดไปใช้งานได้")
        st.header("✅ ผลลัพธ์:",divider=True)
        st.text("🔻โมเดลที่ดีที่สุด คือ KNN โดยใช้ GridSearch หาค่า k ที่เหมาะสม")
        st.text("🔻Random Forest อาจเป็นอีกตัวเลือกที่ดี แต่ KNN ดูจะให้ผลลัพธ์ที่เหมาะสมกว่า")
        st.header("2️⃣ โมเดลที่ใช้กับ Churn Modeling.csv  💳",divider=True)
        st.text("🔻ปัญหาที่แก้:")
        st.text("🔻คาดการณ์ว่าลูกค้าของธนาคารจะ เลิกใช้บริการ (Exited = 1) หรือไม่ (Exited = 0)")
        st.header("🔹 ขั้นตอนการพัฒนาโมเดล:",divider=True)
        st.text("🔻โหลดและเตรียมข้อมูล")
        st.text("🔻แปลงค่าข้อมูลที่เป็นข้อความ (Geography, Gender) เป็นตัวเลขด้วย Label Encoding")
        st.text("🔻ใช้ StandardScaler ปรับขนาดข้อมูล")
        st.text("🔻แบ่งข้อมูลเป็น X (Features) และ y (Target = Exited)")
        st.text("🔻แบ่งชุดข้อมูล (Train/Test Split)")
        st.text("🔻ใช้อัตรา 70% ฝึกสอน (Train) และ 30% ทดสอบ (Test)")
        st.text("🔻พัฒนาโมเดล Deep Learning")
        st.text("🔻ใช้ Neural Network (Deep Learning)")
        st.text("🔻โครงสร้างโมเดล:")
        st.text("🔻Dense Layer: 128, 64, 32, 16 Nodes + ReLU Activation")
        st.text("🔻Dropout 30% เพื่อลด Overfitting")
        st.text("🔻L2 Regularization ในบางเลเยอร์")
        st.text("🔻เลเยอร์สุดท้ายเป็น Linear Activation เพื่อทำนายค่าออกมาเป็นตัวเลข")
        st.text("🔻วัดผลโมเดล")
        st.text("🔻ใช้ Mean Squared Error (MSE), R² Score")
        st.text("🔻พล็อตกราฟแสดง Training Loss vs Validation Loss เพื่อติดตามการเรียนรู้ของโมเดล")
        st.text("🔻บันทึกโมเดล")
        st.text("🔻ใช้ plot_model() แสดงโครงสร้างของโมเดล")
        st.text("🔻โมเดลนี้สามารถนำไปใช้พยากรณ์ในระบบจริงได้")
        st.header("✅ ผลลัพธ์:",divider=True)
        st.text("🔻โมเดล Neural Network สามารถเรียนรู้ข้อมูลได้ดี โดยลดปัญหา Overfitting ด้วย Dropout และ Regularization")
        st.text("🔻สามารถนำไปปรับปรุงต่อโดยใช้ Hyperparameter Tuning หรือทดลองเพิ่ม-ลดจำนวนชั้นของ Neural Network")
        st.header("📌 สรุป:",divider=True)
        st.text("🔻ถ้าต้องการพยากรณ์ ราคาหุ้น/น้ำมัน/ทอง -> ใช้ KNN หรือ Random Forest")
        st.text("🔻ถ้าต้องการพยากรณ์ พฤติกรรมลูกค้า (Churn) -> ใช้ Deep Learning (Neural Network)")
    with tabs[2]:
        st.header("📡Development Model (กระบวนการสร้างโมเดล) ของทั้งสองโมเดล",divider=True)
        st.text("1️⃣ โมเดลทำนายราคาทองและน้ำมันFINAL_USO.csv)")
        st.text("🔍 เป้าหมาย: พยากรณ์ราคาปิดที่ปรับปรุงแล้ว (USO_Adj Close) โดยใช้ข้อมูลราคาหุ้นและดัชนีตลาด")
        st.header("💻กระบวนการพัฒนาโมเดล",divider=True)
        st.text("🔻การเตรียมข้อมูล")
        st.text("🔻โหลดข้อมูลจาก FINAL_USO.csv")
        st.text("🔻ลบคอลัมน์ Date (เพราะไม่มีผลต่อการพยากรณ์)")
        st.text("🔻ตรวจสอบและจัดการค่า Missing Values (ข้อมูลที่หายไป)")
        st.text("🔻แยกข้อมูลเป็น X (features) และ y (target)")
        st.text("🔻การแบ่งชุดข้อมูล (Train/Test Split)")
        st.text("🔻แบ่งข้อมูลเป็น 80% สำหรับฝึก (Train) และ 20% สำหรับทดสอบ (Test)")
        st.text("🔻การปรับขนาดข้อมูล")
        st.text("อใช้ StandardScaler เพื่อปรับค่าข้อมูลให้อยู่ในช่วงมาตรฐาน")
        st.header("🔧การสร้างและฝึกโมเดล",divider=True)
        st.text("🔻ทดลองหลายโมเดลเพื่อหาโมเดลที่ดีที่สุด:")
        st.text("🔻Linear Regression")
        st.text("🔻Decision Tree Regressor")
        st.text("🔻Random Forest Regressor")
        st.text("🔻Support Vector Machine (SVR)")
        st.text("🔻K-Nearest Neighbors (KNN)")
        st.text("🔻ใช้ GridSearchCV เพื่อค้นหาค่า n_neighbors ที่เหมาะสมสำหรับ KNN")
        st.header("📝การวัดผลและเปรียบเทียบโมเดล",divider=True)
        st.text("🔻ใช้ตัวชี้วัด:")
        st.text("🔻Mean Absolute Error (MAE)")
        st.text("🔻Mean Squared Error (MSE)")
        st.text("🔻R² Score")
        st.text("🔻พล็อตกราฟ Actual vs Predicted เพื่อตรวจสอบผลลัพธ์")
        st.text("🔻พล็อตกราฟ R² Score เทียบกับจำนวน neighbors ของ KNN")
        st.text("🔻บันทึกและใช้งานโมเดล")
        st.text("🔻บันทึกโมเดลที่ดีที่สุด (knn_model.pkl)")
        st.text("🔻บันทึกตัวปรับสเกล (scaler.pkl)")
        st.header("🔹 ผลลัพธ์ของโมเดล",divider=True)
        st.text("🔻KNN เป็นโมเดลที่ดีที่สุด โดยค่า n_neighbors ที่เหมาะสมถูกหาโดย GridSearchCV")
        st.text("🔻โมเดลสามารถพยากรณ์ราคาน้ำมัน USO ได้ดี โดยมีค่า R² Score สูงกว่าโมเดลอื่น ๆ")
        st.text("2️⃣ โมเดลทำนายการเลิกใช้บริการของลูกค้า (Churn Modeling.csv)")
        st.text("🔍 เป้าหมาย: ทำนายว่าลูกค้าธนาคารจะ เลิกใช้บริการ (Exited = 1) หรือไม่ (Exited = 0)")
        st.header("🔹 กระบวนการพัฒนาโมเดล",divider=True)
        st.text("🔻การเตรียมข้อมูล")
        st.text("🔻โหลดข้อมูลจาก Churn Modeling.csv")
        st.text("🔻ลบคอลัมน์ RowNumber เพราะไม่มีผลต่อการพยากรณ์")
        st.text("🔻ตรวจสอบข้อมูล Missing Values และการกระจายตัวของข้อมูล")
        st.text("🔻ใช้ Label Encoding แปลงค่าตัวอักษร (Geography, Gender) เป็นตัวเลข")
        st.text("🔻การวิเคราะห์ข้อมูล (EDA)")
        st.text("🔻ใช้ Seaborn และ Plotly เพื่อสร้างกราฟ heatmap และ scatter plot")
        st.text("🔻ตรวจสอบ Correlation Matrix เพื่อดูความสัมพันธ์ระหว่างตัวแปร")
        st.header("⚙️การปรับขนาดข้อมูล",divider=True)
        st.text("🔻ใช้ StandardScaler เพื่อปรับค่าข้อมูลให้อยู่ในช่วงมาตรฐาน")
        st.text("🔻การแบ่งชุดข้อมูล (Train/Test Split)")
        st.text("🔻แบ่งข้อมูลเป็น 70% Train และ 30% Test")
        st.text("🔻การสร้างและฝึกโมเดล Deep Learning")
        st.text("🔻ใช้ Neural Network (Deep Learning)")
        st.text("🔻โครงสร้างของโมเดล:")
        st.text("🔻Dense Layer 128, 64, 32, 16 Nodes + Activation Function ReLU")
        st.text("🔻Dropout 30% เพื่อป้องกัน Overfitting")
        st.text("🔻ใช้ L2 Regularization ในบางเลเยอร์")
        st.text("🔻เลเยอร์สุดท้ายเป็น Linear Activation เพื่อทำนายค่าออกมาเป็นตัวเลข")
        st.text("🔻ใช้ Adam Optimizer (learning rate = 0.001)")
        st.text("🔻ใช้ Mean Squared Error (MSE) เป็น loss function")
        st.text("🔻การวัดผลและวิเคราะห์โมเดล")
        st.text("🔻ใช้ตัวชี้วัด:")
        st.text("🔻Mean Squared Error (MSE)")
        st.text("🔻R² Score")
        st.text("🔻พล็อตกราฟ Training Loss vs Validation Loss เพื่อติดตามการเรียนรู้ของโมเดล")
        st.text("🔻แสดงโครงสร้างโมเดลโดยใช้ plot_model()")
        st.header("🔹 ผลลัพธ์ของโมเดล",divider=True)
        st.text("🔻Neural Network ทำงานได้ดี ในการทำนาย Exited โดยมีการใช้ Dropout และ L2 Regularization เพื่อลด Overfitting")
        st.text("🔻สามารถนำไปปรับปรุงเพิ่มเติมได้โดยใช้ Hyperparameter Tuning หรือเพิ่ม-ลดจำนวนเลเยอร์ของโมเดล")
        st.header("📌 สรุป:",divider=True)
        st.text("🔻ถ้าต้องการ พยากรณ์ราคาน้ำมัน → ใช้ KNN หรือ Random Forest")
        st.text("🔻ถ้าต้องการ พยากรณ์พฤติกรรมลูกค้า (Churn) → ใช้ Deep Learning (Neural Network)")
    with tabs[3]:
        st.header("📌 ทฤษฎีที่ถูกใช้ในกระบวนการสร้างโมเดล",divider=True)
        st.text("🔷โมเดลที่พัฒนาในโค้ดนี้เกี่ยวข้องกับการพยากรณ์ค่าต่อเนื่อง (Regression) โดยมีการใช้ทฤษฎีดังต่อไปนี้:")
        st.text("1️⃣ การเตรียมข้อมูล (Data Preprocessing)")
        st.text("🔷Standardization: ใช้ StandardScaler ในการปรับข้อมูลให้อยู่ในช่วงมาตรฐาน (mean = 0, std = 1) เพื่อให้โมเดลเรียนรู้ได้ดีขึ้น")
        st.text("🔷Feature Selection: เลือกฟีเจอร์ที่เกี่ยวข้อง เช่น Open, High, Low, Volume เป็นตัวแปรอิสระ และ USO_Adj Close เป็นค่าที่ต้องการพยากรณ์")
        st.text("2️⃣ ทฤษฎีเกี่ยวกับโมเดลที่ถูกนำมาใช้")
        st.text("🔷ในโค้ดนี้ มีการทดลองโมเดลหลายแบบ ได้แก่:")
        st.text("🔷Linear Regression (LR)")
        st.text("🔷ใช้สมการเส้นตรง")
        st.text("𝑦")
        st.text("=")
        st.text("𝑚")
        st.text("𝑥")
        st.text("+")
        st.text("𝑐")
        st.text("y=mx+c เพื่อพยากรณ์ค่า")
        st.text("🔷เหมาะสำหรับข้อมูลที่มีความสัมพันธ์เชิงเส้นตรง")
        st.header("Decision Tree Regressor (DT)",divider=True)
        st.text("🔷ใช้โครงสร้างต้นไม้ตัดสินใจ (Decision Tree) เพื่อแบ่งกลุ่มข้อมูล")
        st.text("🔷สามารถเรียนรู้ความสัมพันธ์ที่ซับซ้อนได้ แต่มีโอกาสเกิด Overfitting ได้ง่าย")
        st.header("Random Forest Regressor (RF)",divider=True)
        st.text("🔷ใช้ Ensemble Learning โดยรวมหลาย ๆ ต้นไม้เข้าด้วยกันเพื่อเพิ่มความแม่นยำ")
        st.text("🔷ลด Overfitting ที่พบใน Decision Tree")
        st.header("Support Vector Regression (SVR)",divider=True)
        st.text("🔷ใช้ Support Vector Machine (SVM) สำหรับปัญหาการพยากรณ์")
        st.text("🔷สร้างเส้นขอบเขต (Hyperplane) ที่พยายามพยากรณ์ค่าภายในระยะขอบที่เหมาะสม")
        st.text("🔷ทำงานได้ดีเมื่อตัวแปรมีความสัมพันธ์ที่ไม่เป็นเชิงเส้น")
        st.header("K-Nearest Neighbors (KNN)",divider=True)
        st.text("🔷ใช้ค่าของ k เพื่อนำค่าจากจุดใกล้เคียงมาหาค่าเฉลี่ยสำหรับพยากรณ์ค่าใหม่")
        st.text("🔷ค้นหาค่า k ที่เหมาะสมโดยใช้ GridSearchCV")
        st.header("🔍 สุดท้ายเลือกใช้โมเดลไหน และทำไม?")
        st.text("✔ โมเดลที่เลือกใช้สุดท้ายคือ: K-Nearest Neighbors (KNN)")
        st.header("📌 เหตุผลที่เลือก KNN",divider=True)
        st.text("🔷ทดสอบหลายโมเดลแล้ว KNN ให้ผลลัพธ์ดีที่สุด")
        st.text("🔷ใช้ R² Score และพบว่า KNN มีค่าความแม่นยำสูงสุด")
        st.text("🔷ค่าความผิดพลาด MAE, MSE ต่ำกว่าตัวอื่น")
        st.text("🔷ค้นหาค่า n_neighbors ที่เหมาะสมด้วย GridSearchCV")
        st.text("🔷ทำให้มั่นใจว่าเลือกค่า k ที่เหมาะสมที่สุด")
        st.text("🔷ค่าที่ได้ช่วยเพิ่มประสิทธิภาพของโมเดล")
        st.text("🔷โมเดลมีความยืดหยุ่นสูง และไม่ Overfit ง่าย")
        st.text("🔷Random Forest อาจ Overfit กับข้อมูลบางประเภท")
        st.text("🔷KNN สามารถปรับค่า k เพื่อหลีกเลี่ยง Overfitting ได้ง่าย")
        st.header("📌 สรุป",divider=True)
        st.text("🔷ทฤษฎีที่ใช้: Regression, Standardization, Model Selection (GridSearch)")
        st.text("🔷โมเดลที่ทดลอง: Linear Regression, Decision Tree, Random Forest, SVR, KNN")
        st.text("🔷โมเดลสุดท้ายที่เลือก: KNN (ค่าที่เหมาะสมที่สุดจาก GridSearch)")
    with tabs[4]:
        st.header("📌การเตรียมข้อมูล (Data Preprocessing)",divider=True)
        st.text("🔶การใช้ Label Encoding เพื่อแปลงค่าข้อมูลประเภท categorical ให้เป็นตัวเลข")
        st.text("🔶การใช้ StandardScaler เพื่อปรับขนาดของข้อมูลให้มีการกระจายที่เหมาะสมกับโมเดล")
        st.text("🔶การวิเคราะห์ข้อมูลเบื้องต้น (Exploratory Data Analysis - EDA)")
        st.text("🔶ใช้ Seaborn และ Plotly เพื่อวิเคราะห์ข้อมูลด้วย heatmaps, pair plots, histograms ฯลฯ")
        st.text("🔶การใช้ Bokeh และ Plotly เพื่อสร้าง interactive visualization")
        st.text("🔶การแบ่งข้อมูล (Train/Test Split)")
        st.text("🔶ใช้ train_test_split แบ่งข้อมูลเป็นชุดฝึก (Training Set) และชุดทดสอบ (Test Set) ในอัตรา 70:30")
        st.header("📌การใช้ปัญญาประดิษฐ์ (Deep Learning)",divider=True)
        st.text("🔻สร้างโมเดล Neural Network โดยใช้ TensorFlow/Keras")
        st.text("🔻ใช้โครงสร้าง Sequential Model ที่ประกอบด้วย")
        st.text("🔻Dense Layers พร้อม Activation Function ReLU")
        st.text("🔻การใช้ L2 Regularization เพื่อลด Overfitting")
        st.text("🔻ใช้ Dropout เพื่อลดการ Overfit โดยปิดใช้งานบางส่วนของ neurons แบบสุ่ม")
        st.text("🔻เลเยอร์สุดท้ายใช้ Activation Linear เพื่อทำการทำนายค่าต่อเนื่อง")
        st.header("📌ฟังก์ชันที่ใช้ในการวัดผล",divider=True)
        st.text("🔷Mean Squared Error (MSE): ใช้วัดความคลาดเคลื่อนของค่าที่ทำนาย")
        st.text("🔷R² Score: ใช้วัดคุณภาพของโมเดลในการทำนาย")
        st.header("📌โมเดลสุดท้ายที่เลือกใช้",divider=True)
        st.text("🔷โมเดลที่เลือกใช้คือ Deep Neural Network (DNN)")
        st.text("🔷มีโครงสร้างเลเยอร์ที่ลึกขึ้นพร้อมการใช้ Regularization และ Dropout เพื่อลด Overfitting")
        st.text("🔷ใช้ Adam Optimizer พร้อม learning rate 0.001")
        st.text("🔷Loss Function ที่ใช้คือ Mean Squared Error (MSE)")
